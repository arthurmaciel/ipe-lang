# Backlog

Durable open-work items. One line each; newest at the bottom.

- MUST-FIX REVIEW — direct IR→WASM backend (`ipe_backend_wasm`, spec in `docs/architecture/tbd/wasm-backend.md`): rejected as unbuildable-as-scoped. Headline blocker: the vendored rustc-origin `runtime.wasm` (linear memory) cannot interoperate with the proposed WASM-GC app for any heap-carrying, generic, or higher-order kernel — so runtime-reuse (the plan's cost-bounding thesis) does not exist and the 4-5-month MVP is mis-costed. Needs a single coherent app↔runtime memory story proven on one non-flat value before re-review.
- IN PROGRESS — native Rust formatter (approved-approach spec in `docs/architecture/tbd/formatter-315-spec.md`): a Wadler Doc-algebra emitter built during the IR walk, replacing the per-file `rustfmt` subprocess fork — for soundness (structural token-preservation, no re-lex), speed, and fmt-clean-by-construction (the CI fmt gate can never red on emitted output). Approach panel-endorsed; rustfmt layout rules settled empirically against the 483-golden byte corpus (goldens never re-blessed). Remaining phases: P0 chain/renderer byte-gate → P1 emit_expr + framework builders → cutover (delete `run_rustfmt`) → `[fmt]` config → wasm32 no-`std::process` proof.
- FFI SSOT dedup — the crate-version TOML-value charset `[0-9A-Za-z.*=<>~^,+ -]` is hand-rolled twice (`VersionPin` in `ffi/driver.rs`, `CrateVersion` in `ffi/pkginfo.rs`); consolidate to one shared charset predicate, mirroring the `FeatureName` consolidation. Keep the two newtypes (distinct error types and decode boundaries) — share only the predicate.
- FFI feature gaps from the bevy exercise (detail in `examples/sky/BLOCKERS.md`): auto-binding drops `Bundle`-generic methods (`spawn`/`insert`), user-defined `Component` types (Ipê cannot define a Rust type), and `dyn Fn`/`FnMut` system closures — the shim-free model's honest limits, each fail-closed (dropped binding), not SEAL breaks.
