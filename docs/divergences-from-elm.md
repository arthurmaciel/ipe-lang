# Divergences from Elm — superseded

> **Reframed and moved.** This ledger was renamed to
> [`architecture/extensions-to-elm.md`](architecture/extensions-to-elm.md) and
> reframed around **coverage of the official `elm/*` API** ("extensions to Elm"),
> not "divergences from Elm". The new doc carries the full coverage matrix (every
> `elm/*` module → per-value status: present / diverged / missing / excluded),
> the deliberate-departure narrative (effect model, typed `Error`, server-driven
> TEA, native stdlib), and the justified exclusions.
>
> The prioritized plan for closing the relevant gaps lives in
> [`architecture/tbd/elm-coverage-gap-plan.md`](architecture/tbd/elm-coverage-gap-plan.md);
> the exhaustive per-value `elm/core` table lives in
> [`architecture/tbd/elm-core-coverage.md`](architecture/tbd/elm-core-coverage.md).
>
> This stub is kept only so existing inbound links resolve; read the new doc.
