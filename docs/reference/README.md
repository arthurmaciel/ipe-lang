# Reference

Generated API reference. [`stdlib.md`](stdlib.md) is the standard-library
index — every module with a one-line summary per documented symbol, each linked
to a per-module detail page under [`stdlib/`](stdlib/).

These files are generated from the stdlib doc-strings by `gen-stdlib-docs`; do
not edit them by hand. A drift gate in CI regenerates them and fails on any
difference.
