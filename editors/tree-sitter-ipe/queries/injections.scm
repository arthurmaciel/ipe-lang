; Language injections for Ipê.
;
; A `{-| … -}` doc comment may embed fenced Ipê code blocks (```ipe … ```),
; but there is no separate markup language to inject; doc comments are plain
; prose. Left minimal by design — add injections here if a future construct
; embeds a foreign language (e.g. inline SQL / HTML).
