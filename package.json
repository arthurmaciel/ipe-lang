{
  "name": "ipe-verify-tooling",
  "private": true,
  "type": "module",
  "description": "Dev-tooling deps for the Go≡Rust equivalence drivers under scripts/equivalence-checks/ (web-verify.mjs behavioural round-trip + screenshot-compare.mjs visual pixel diff — see scripts/lib/checks.sh's exercise_live). Not part of the compiler/runtime; node_modules is gitignored.",
  "devDependencies": {
    "pixelmatch": "^6.0.0",
    "playwright": "^1.49.0",
    "pngjs": "^7.0.0"
  }
}
