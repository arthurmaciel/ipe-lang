# BACKLOG — open work items (SSOT)

> The single authoritative list of **pending** work for the Ipê (Sky→Rust)
> compiler, backend, and runtime. Done work is not recorded here — the done
> ledger lives in [`ROADMAP.md`](ROADMAP.md), which also defines the road-map
> phases used in this table. Read on demand; update as items land.
>
> **Progressive-development loop contract:** rows whose **Notes** column
> contains the literal tag `[progdev-safe]` are loop-eligible (mechanical,
> reference-backed, no design decision). The loop removes a row when it lands
> the item, or updates the row's Notes to record the new blocker.
>
> **Soundness/security/correctness outrank everything** — never close an item
> with a workaround (root-cause fixes only; no-deferral). Priorities:
> Critical > High > Medium > Low.

| Priority | Road map phase | Task | Notes | Spec |
|---|---|---|---|---|
| High | Sweep to green | SEAL violation in `examples/24-tui-kitchen-sink` emit — skyc exits 0 but the emitted `sky-app` fails `cargo build` with 2 × E0308. (1) `src/main.rs` string-literal pattern on a `SkyMaybe<String>` scrutinee emits `SkyMaybe::Just("live")` — a `&str` pattern against a `String` payload (`expected String, found &str`); the pattern arm needs owned-String matching (guard `s.as_str() == "live"` or `String::from` at emit). (2) `src/main.rs` entry wrapper calls `block_on(sky_main())` where `sky_main()` returns `SkyResult<SkyError, ()>` but `block_on` expects a `SkyTask<E,A>` (`Pin<Box<dyn Future…>>`) — the Sky.Tui `main = Tui.app cfg |> Task.run` entry-contract mis-shapes the top-level future (`task.rs:5`). Two independent codegen defects on the tui-app entry path. | Found 2026-07-11 while verifying the ported `scripts/equiv-render.sh` tui-grid driver (#110 §3.2) against `24-tui-kitchen-sink` — the Rust build of the emitted crate is the failure. Repro: `cd examples/24-tui-kitchen-sink && rm -rf sky-out && skyc build sky.toml --out sky-out/rust && cargo build --manifest-path sky-out/rust/Cargo.toml`. PRE-EXISTING (not caused by this lane, which touches only runtime CSS-safety + CI scripts, zero codegen). Verified against a skyc built at base `fb32dd3`; master's only advance since is salsa/parse (no tui-emit codegen change), so it reproduces on current master. Blocks declaring `24-tui-kitchen-sink` sweep-green AND the #110 tui-grid EQUIV run. | |
| Medium | Post-completion | #56b Row-var record annotation syntax `{ r \| f : T }` does not parse (SKY-P0001) — reference accepts + monomorphises the callee per record shape | Completeness gap found while investigating #56. Zero corpus usage (not sweep-blocking). Needs per-record-shape callee monomorphisation in the backend first (Rust generics can't express row field access) — same machinery the class-1 coupling tripwire (§Coupling tripwire) needs; land together if possible. Canary test `row_poly_annotation_gap` (from #56) asserts SKY-P0001 today. | `docs/architecture/row-poly-subset-superset-design.md#gap-filed-row-var-record-annotation-syntax-r--f--t` |
| Medium | Security hardening | CSS style-marker typed-variant hardening — make marker forgery UNREPRESENTABLE: a typed `Attribute::StyleMarker` variant (carrying `SafeCss*`-proven payloads) that `Ui.htmlAttribute`/`Html.attribute` cannot construct, replacing the raw `data-sky-{mq,pc,tr,anim}-*` string attrs across all four builders | NON-EXPLOITABLE since 2026-07-11: ALL FOUR sinks (`build_mq`/`build_pc`/`build_tr`/`build_anim`) now re-validate their marker payloads at the sink through the shared `css_safety` policy and drop fail-closed (`build_anim` closed last, via the keyframe-grammar validator `sink_safe_keyframes_body`). This row is the remaining make-invalid-states-unrepresentable refactor, defence-in-depth only. Needs a DESIGN pass first: `html::Attribute` / `ui::element::Attribute` mirror the Sky stdlib ADTs by name/position (codegen bridge), so a Rust-only variant risks non-exhaustive matches in emitted code (SEAL hazard) — the variant must stay invisible to the Sky-side ADT contract. | `docs/architecture/ui-mediaquery-design-2026-07-11.md` |
| Medium | Security hardening | **DEFERRED (explicit user override, 2026-07-10) — not blocking this campaign.** #66-T2 Type-directed well-typed AST generator (fuzzer Tier-A): generate arbitrary well-typed programs by construction (typing rules run in reverse), assert skyc accepts + emitted program is no-panic | Guardian-typesystem, multi-session PROJECT — do NOT rush. Adopt the reference design (`../sky` `WellTypedFuzzerGen.hs`) via `proptest`/`arbitrary`. Load-bearing invariant: "well-typed by construction under generation". Scope: pure type-relevant constructs only. | |
| Medium | Security hardening | **DEFERRED (explicit user override, 2026-07-10) — not blocking this campaign.** #66-N second half — differential rejection fuzzer vs `../sky`: mutate freely, run both compilers, compare accept/reject | The reference is NOT ground truth: a divergence is a REVIEW candidate, never an auto-verdict ("Ipê rejects, Sky accepts" is most likely Ipê being MORE correct). First half (guaranteed-breaking mutation tier, `scripts/fuzz-ill-typed.sh`) landed 2026-07-06. | |
| High | CI, oracle & publish | #35 Port examples-sweep to skyc + run the full sweep (the source-of-truth gate) | Porting itself is done (spec §1). Declaring "sweep-green" is still blocked on the Critical Boundary-Scheme-Promotion (class-1) row above — do not run the gating full sweep until that lands (spec §0). 2026-07-10: fixed the reproduced same-`CARGO_TARGET_DIR` binary-swap race (`flock`-guarded critical section + PID-suffixed report stamp, commit `6d93e85`) — independent review confirmed this part is correctly implemented. Residual gap found by the SAME review: see the new row below — do not declare the sweep gate fully trustworthy until it lands. | `docs/architecture/class2-tier1-sweep-fix-spec-2026-07-09.md` |
| High | CI, oracle & publish | #110 Oracle full-activation — REMAINING: (1) SVG-coord Rule-1 hardening in `equiv_normalize_html.py` (the blanket `SVG_COORD`→`#` mask is a false-green hole for a skyc SVG-coord regression — compare against a stored snapshot or drop the mask, spec §3.1); (2) browser/scenario stack — `package.json` + Playwright + `scripts/web-verify.mjs`/`verify-scenarios.mjs` so a `live`-shape example's EQUIV reaches `equiv-scenario` instead of the boot-floor fallback (spec §3.3); (3) rebuild + pin the RELEASE skyc (spec §3.4); (4) flip CI to phase-2 — the `examples-sweep-equiv` job (spec §3.5); (5) 65-fixture non-FFI divergence corpus + `scripts/equiv-corpus.sh`/`equiv-render.sh`-corpus drivers (spec §3.6). | tui-grid render-diff driver LANDED 2026-07-11: `scripts/equiv-render.sh` ported from `../sky/runtime-rust/scripts/equiv-render.sh` (spec §3.2 — the one ❌ item with no inline prior-art, wired via the standalone driver by the reference's own architecture, NOT inlined into examples-sweep.sh's pty case). Adapted to this repo's two-binary shape: pinned Go oracle (`SKY_GO_BIN`, `env -u SKY_RUNTIME_DIR` on its build) + `skyc` (`SKYC_BIN`) + `cargo build` of the emitted crate + `resolve_bin` from `lib/checks.sh`. Each runtime path verified independently exit-0 (Go-oracle build, skyc emit, cargo build, pyte present); a single one-shot green `tui 24-tui-kitchen-sink` run was build-time-starved under multi-lane cargo-lock contention with cold caches (`timeout 124` on the reference's own `timeout 300`/`600` budgets), NOT a script defect — re-run when caches are warm / other lanes idle. SEAL-safe (verification harness, not compiler code — cannot affect skyc exit-0⇒cargo-builds). | `docs/architecture/class2-tier1-sweep-fix-spec-2026-07-09.md` |
| High | CI, oracle & publish | #37 Fix CI (port ../sky examples-sweep.yml + ci.yml) + push to `git@github.com:arthurmaciel/ipe-lang` | Includes the CI example-patch-queue (in-repo patches CI applies to upstream examples before build), accepted per `docs/divergences-from-sky.md#planned-future-divergences`. Windows question: `docs/architecture/tui-windows-ci.md` / `docs/architecture/windows-ci-support.md`. Plans: `docs/architecture/sweep-and-parity-plan.md`, `docs/superpowers/plans/2026-07-02-ci-and-push.md`. | `docs/architecture/class2-tier1-sweep-fix-spec-2026-07-09.md` |
| Critical | CI, oracle & publish | #59 PRE-PUSH: full codebase rename Sky → Ipê/Ipe/ipe (case-preserving; watch the naive-sed trap — upstream-Sky refs stay Sky) | Runs SOLO, dead-last before the push — no other work in flight during the rename. | `docs/superpowers/plans/2026-07-03-rename-sky-to-ipe.md` |
| Medium | CI, oracle & publish | Publish the README (honest relation-to-Elm-and-Sky framing) | Re-run the divergences review (`docs/divergences-review.md`) first so the ledger the README cites is current. | `docs/README-draft-relation-to-elm-and-sky.md` |
| High | CI, oracle & publish | Gate blind spot — `cargo nextest run --workspace` does NOT enable the runtime's `live` feature (feature-unification from the workspace's other crates doesn't pull it), so the ENTIRE `sky_runtime::live::*` test surface (`style_inject` CSS-injection sink gates, SSE/session/dispatch tests) is silently excluded from the default gate. A default-feature lane shows green while `live` tests may be red (empirically: the 2026-07-11 CSS-marker sink hardening's 3 updated tests + 5 new forgery gates only run under `--features live`). Fix: add an explicit `--features live` (or `--all-features` / `--features full`) runtime lane to CI AND to the local gate recipe, so the live surface is actually exercised. Until then, every gate that touches `live/**` MUST be run manually with `--features live`. | Found by independent adversarial review (agent `aeec39f`) of the CSS-sink hardening, 2026-07-11. | |
| Medium | Hardening follow-ups | #31 Phase-4 remainder — make-invalid-states-unrepresentable hardening (non-blocking) | | |
| Medium | Hardening follow-ups | AUD-09 remaining leftover: Bug-29 — the `any`-return UI-msg injection heuristic (`any_ui_msg_injection`, `crates/sky_lower/src/lower.rs` ~4332) matches ANY single-arg `Ty::Con` whose arg is a bare `Ty::Var`, never checking the Con NAME — so it also fires for `Maybe`/`List`/`Set`/`Decoder` (`w : Int -> any; w n = []` → `fn main_w<T1>() -> Vec<T1>`, generic only in return → cargo E0282 at an unpinning call site). Fix per audit: gate the injection on the Con being a UI msg-parametric ctor (Html/Element/Attribute); otherwise pin to the concrete carrier or fail closed. | GUARDIAN-DESIGN (campaign classification Class 1 — inference cluster). Short spec WRITTEN 2026-07-11 (name-gate on Html/Element/Attribute; non-UI shapes fail closed with SKY-L0102; records a divergence — Go accepts `w n = []` under `any`). Repro at HEAD confirms the name-blind injection emits `fn main_w<T1>() -> Vec<T1>`; the E0282 breach is call-site-dependent (the simple `let _ = w 1` probe compiled), so this is a latent SEAL trap, not a live sweep blocker. The row's other two sub-items landed 2026-07-11: `from_parts_unchecked` replaced by sealed `Match::map_bodies`/`try_map_bodies`/`map_scrutinee` combinators (+ source seal test), and the unconditional field-access `.clone()` closed with #142. | `docs/architecture/bug29-any-return-ui-injection-gate-2026-07-11.md` |
| Low | Hardening follow-ups | No default-gate (non-`SKY_E2E`) test pins that `ir_type_from_canon`'s anonymous-record-annotation path and `ir_type_from_ty`'s structural-fold path agree on `IrType::HttpRequest` for the same 7-field shape — the only test exercising this (`m5b_http_builders`, which caught the original two-path divergence) is `SKY_E2E`-gated and invisible to `cargo nextest run --workspace` | Found by independent adversarial review (commit `31beb85`, the annotation-path fix for the Critical name-only-fold row above). Fix itself verified correct (nextest 2025/2025, clippy clean, `SKY_E2E=1` golden passes) — this is a coverage gap only: a future regression on either path would ship silently past the default gate. Add a fast emit-text assertion (no runtime build needed) pinning both paths to the same `IrType::HttpRequest`. | |
| Low | Hardening follow-ups | #53 Emit backend via typed token AST instead of String concatenation | Guardian-design item. | |
| Low | Hardening follow-ups | `parity_matrix_zero_mismatches`'s `find_binary()` picks up a stale cached `target/{debug,release}/parity-matrix` binary predating a `git merge master` into a long-lived worktree, so a fully up-to-date `KNOWN_DEAD_OR_EPILOGUE` allowlist (source-correct) still reports spurious MISMATCH rows — false gate failures, not real bugs | Bit twice this session (tui-SEAL, http-fields lanes) — both traced to `cargo build -p parity-matrix` never being re-run after a merge, since `find_binary()` only checks mtime-agnostic existence. Fix: make the test's `find_binary()` also invoke (or the CI gate recipe run) `cargo build -p parity-matrix` before `extract`, or add a cheap staleness check (binary mtime vs `tools/parity-matrix/src/main.rs` mtime). | |
| Low | Hardening follow-ups | Efficiency-audit residual (7 gated findings; the 20 strictly-safe ones landed 2026-07-11): §7 `ModPathId` (determinism/`Ord` guard), §2 scope persistent map (profile-gated), §3 `lower_callee` symbol table (broad diff), §4 Http consts + `record_by_fieldset` keying (shared-keying correctness gate), §5 lexer streaming (byte-identical golden gate), §6 `SafeCssValue` lazy (security-gated) | See ledger §0 burn-down status for what landed vs why each residual is gated. | `docs/architecture/efficiency-audit-2026-07-02.md` |
| Medium | Sweep to green | `let f = Ok (\\x -> x * 2)` — a let-bound constructor-wrapped closure later passed to a `Box<dyn Fn>`-expecting position — is a skyc-accept / cargo-reject seal hole (E0308): the emitted `let f = SkyResult::Ok(Box::new(move \|x\| ...));` never unsize-coerces `Box<{closure}>` to `Box<dyn Fn>` across the `let` boundary (the inline pipe form works because `ok_res(...)`/`result_and_map(...)` supply the coercion target at the call site; a bare `let` does not) | Found by the #90 5th-attempt adversarial review (2026-07-11), **bisected to `cd9bb1c`** (the attempt-4 Stage-1 T1/T2 ctor-payload lift) — entirely independent of the T3 arity gate and the error-slot pin, both verified sound. Same "no general coerce-on-let pass" class as `ctor-payload-function-design.md` §1.2's hazard table, manifesting as E0308-at-let rather than E0382-at-reuse. Coverage gap: no positive golden uses the `let f = Ok/Just (\\...)` shape. | `docs/architecture/ctor-payload-function-design.md` |
| Low | Hardening follow-ups | Decoder reuse via a binder whose type nests a `Decoder` inside `Dict`/`Set`/`Enum`/`Task` (containers `ir_type_contains_decoder` / #89's `decoder_ir_type` don't recurse into) is a skyc-0 → cargo-101 (`Decoder: Clone` E0277/E0599) hole — the backend clones the `!Clone` container arg | Found by the #125 review (2026-07-11), PROVED pre-existing (not a #125 regression): the identical `Dict String (Decoder a)` failure reproduces via a BARE `let decoders = …` with no destructure at all, because #89's own PVar decoder-thunk path only matches a bare `IrType::Decoder`, never a Decoder nested in a non-recursed container. #125 faithfully matched #89's reach (Tuple/Record/Maybe/Result/List) and no further; spec §2.3 explicitly acknowledged this as "out of today's realistic reach". Enum-wrapped Decoders additionally fail CLOSED at SKY-L0126 (loud, never silent-wrong). A real fix is broader than the gate (the backend still clones the container arg) — extending the gate alone would not close it. | `docs/architecture/class5-emitter-clone-fix-spec-2026-07-09.md` |
| Low | Hardening follow-ups | `golden_mixed_arm_task_run_elision_seal.rs`'s E2E test only exercises the `Ok`/success path of the `sky_main_wrap`-via-`task_from_result` extension (mixed Task.run/Result-arm entry) — no `Err`-path assertion pins that error propagation through `block_on` -> `fn main`'s epilogue -> process exit code stays correct | Found by independent re-review (agent `a3858ec`) of the tui-entry-path fix (`fe1592e`) — the reviewer manually confirmed the `Err` path works correctly via an ad-hoc fixture, but the shipped regression suite doesn't pin it. Non-blocking (mechanism itself verified sound), but a real coverage gap. | |
| High | FFI | #40 FFI Phase 0 — inspector hardening (disjoint `tools/` crate) | | `docs/superpowers/plans/2026-07-02-ffi-phase0-inspector.md` + `docs/architecture/ffi-subsystem-design.md` |
| Critical | FFI | #41 FFI sandbox — blocking security gate before `ipe add` ships (compiling arbitrary third-party crates is execution of untrusted code) | Security precedes any generation work. | `docs/architecture/ffi-sandbox-and-generator-impl-ready.md` |
| High | FFI | #42 FFI consumer port — generator (Haskell → Rust `ipe_ffi` crate) | Depends on the kernel registry. | `docs/architecture/ffi-port-spec.md` + `docs/architecture/ffi-design.md` + `docs/architecture/ffi-rust-subsystem-design.md` |
| Medium | FFI | Async FFI bridge — bind async Rust SDKs (tokio-runtime bridge, `AbortOnDrop` cancel propagation, B8 error funnel) | Shim-free async-SDK binding is THE acceptance metric. | `docs/architecture/async-ffi-bridge-design.md` |
| Medium | Post-completion | #155 Route URL changes to a Msg (Elm `Browser.application` parity), demote the magic `page` field to sugar | First Elm-core-coverage item. | `docs/architecture/url-navigation-msg-design.md` |
| Medium | Post-completion | #116 Entry contract (Option C): auto-run Task/backend-app `main` + drop trailing `\|> Task.run` (v0.17.3 pipeCollapsesTask parity) | | `docs/architecture/adopt-from-sky-v0172.md` |
| Medium | Post-completion | #128 Drop `Task.run` + `Task.perform` from the Ipê surface (#116 companion) | Departure — first consumer of the CI example-patch-queue. | `docs/architecture/drop-task-run-surface-design.md` |
| Medium | Post-completion | #131 `Task.map2..5` + `Task.parallel2..5` (expression forms) + `parallelDo` block | | `docs/architecture/task-combinators.md` |
| Medium | Post-completion | #133 Multiline-string margin stripping (anchor = first string character's column) | Departure — output-changing; records an oracle divergence per patch class. | `docs/architecture/multiline-string-margin-stripping-design.md` |
| Medium | Post-completion | Idea-7 effect `do` block (scoped effect sequencing; kills the `let _ = TaskExpr` auto-force wart) | DESIGNED 2026-07-04. | `docs/ideas/idea-7-effect-do-block-design.md` |
| Medium | Post-completion | C.4 Guarantee Elm `core` library coverage — audit the stdlib against `elm/core`, add missing modules/functions (Array, Tuple, Bitwise, …) | | `docs/architecture/elm-core-coverage.md` + `docs/architecture/elm-core-gap-matrix.md` + `docs/architecture/additive-stdlib-features.md` |
| Medium | Post-completion | C.2 Module-namespace redesign: single flat standard library, nothing imported by default, LSP auto-import on first use | Research prelude handling in Rust, Elm, Gleam, Haskell, Go, Zig first. | `docs/architecture/flat-namespace-redesign.md` |
| Medium | Post-completion | C.3 Source-name de-abbreviation (`kernel_ty` → `kernel_type`, `Ty::Var` → `Type::Variable`; idiomatic Rust abbreviations retained) | | `docs/architecture/readability-and-naming-audit.md` |
| Medium | Post-completion | C.5 Evaluate more principled compilation strategies from the reference Haskell backend + `elm/compiler`; adopt only where a principle strictly improves, else record the comparison table | | `docs/architecture/sky-upstream-learnings.md` |
| Medium | Post-completion | C.6 Implement the filed divergent language features (deep-update sugar, or-patterns, pattern guards, record punning, hot-reload family, time-travel debugger, …) | Divergences go last, on a verified-complete base. | `docs/divergences-from-sky.md#planned-future-divergences` |
| Low | Longer-horizon | D.1 Incremental compilation (salsa) across the compiler and the LSP — foundation for fast watch + hotpatching | Phase 1 LANDED 2026-07-11: `sky_db` salsa (=0.27.2) database + `SourceFile`/`SourceRoot` inputs + `parse`/`imports` tracked queries wired onto the one-shot `skyc` path (byte-identical, SEAL green); memo-hit proof via salsa event log. Phases 2–8 (resolve_imports/interface firewall/coarse spine/parity gate/per-module typecheck/emit bridge/persistence/watch/LSP) remain per the phase table in the 2026-07-11 spec. | `docs/architecture/salsa-incremental-compilation-2026-07-11.md` (Phase-1 impl spec) + `docs/architecture/incremental-compilation-and-watch.md` + `docs/superpowers/plans/2026-07-03-incremental-salsa.md` |
| Low | Longer-horizon | D.2 Standard-library behaviour audit against Elm semantics (JSON key order, integer-decoder strictness, float formatting, null/oneOf/nullable) | | `docs/architecture/stdlib-elm-behaviour-audit-plan.md` |
| Low | Longer-horizon | D.3 Full floating-point Set/Dict keys (ordered-float) + locale-correct case mapping | Lifts SKY-L0117. | `docs/architecture/float-keys-and-locale-case-design.md` |
| Low | Designed targets | E.1 WASM / browser target (TEA in the browser, playground; compile-time public-bundle secret boundary) | | `docs/architecture/wasm-target.md` |
| Low | Designed targets | E.2 Static compilation — portable single binaries (musl / static-CRT; `dlmalloc` default allocator) | | `docs/architecture/static-compilation.md` + `docs/superpowers/plans/2026-07-03-static-compilation.md` |
| Low | Designed targets | E.3 Language server (LSP) — salsa-backed, TEA scaffolding, `VerifiedEdit` gate | | `docs/architecture/ipe-lsp.md` + `docs/superpowers/plans/2026-07-03-lsp.md` |
| Low | Designed targets | E.4 TEA everywhere — opt-in headless `Std.Worker.program` shape for every backend | Implementation invariants recorded in the spec (Acquire-loads before `try_recv`; `select!` over mailbox + quit-notify; abort source tasks during quit-drain). | `docs/architecture/tea-everywhere.md` |
