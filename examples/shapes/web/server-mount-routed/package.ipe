module Package exposing (package)

import Ipe.Package exposing (..)


package : Package
package =
    { name = "server-mount-routed"
    , version = "0.1.0"
    , delivery =
        { desktop = { title = "server-mount-routed", width = 1024, height = 768 }
        , mobile = { bundleId = "com.example.server-mount-routed", orientation = Portrait }
        , browser = { basePath = "/" }
        }
    }
