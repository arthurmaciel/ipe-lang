module Package exposing (package)

import Ipe.Package exposing (..)


package : Package
package =
    { name = "wasm-env-public"
    , version = "0.1.0"
    , wasm =
        On { mode = Spa, publicEnv = [ "API_BASE_URL" ] }
    }
