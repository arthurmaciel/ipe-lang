module Package exposing (package)

import Ipe.Package exposing (..)


package : Package
package =
    { name = "wasm-browser-permission"
    , version = "0.1.0"
    , wasm =
        On { mode = Spa }
    , capabilities =
        { declares = []
        , accepts = [ JsPort Permission ]
        }
    }
