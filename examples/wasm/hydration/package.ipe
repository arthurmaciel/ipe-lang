module Package exposing (package)

import Ipe.Package exposing (..)


package : Package
package =
    { name = "wasm-hydration"
    , version = "0.1.0"
    , wasm =
        On { mode = Hydrate }
    }
