module Zipper exposing (Zipper(..), singleton, current)

type Zipper a
    = Zipper (List a) a (List a)


singleton : a -> Zipper a
singleton x =
    Zipper [] x []


current : Zipper a -> a
current z =
    case z of

        Zipper _ x _ ->
            x
