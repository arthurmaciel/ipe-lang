name = "skychess"
version = "0.1.0"
entry = "src/Main.ipe"

[source]
root = "src"

[live]
port = 8000
store = "memory"

[database]
driver = "sqlite"
path = "skychess.db"

[capabilities]
accept = ["unsafe"]
