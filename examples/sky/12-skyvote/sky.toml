name = "skyvote"
version = "0.1.0"
entry = "src/Main.sky"

[source]
root = "src"

[live]
port = 8000
store = "sqlite"
storePath = "skyvote.db"

[database]
driver = "sqlite"
path = "skyvote.db"

[auth]
method = "password"
secret = "skyvote-dev-secret-change-in-prod"
session_ttl = "24h"
