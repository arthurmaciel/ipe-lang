# Fyne Stopwatch

Desktop GUI stopwatch built with the Fyne toolkit. Demonstrates Sky's FFI bindings for native desktop applications.

## Build & Run

```bash
sky install
sky build src/Main.sky
./sky-out/app
```

Requires Fyne system dependencies (OpenGL, etc.). See [Fyne prerequisites](https://docs.fyne.io/started/) for your platform.
