module Lib.Utils exposing (add, formatMessage)

import Ipe.Prelude exposing (..)
import Ipe.String as String


add : Int -> Int -> Int
add a b =
    a + b


formatMessage : String -> Int -> String
formatMessage msg count =
    msg ++ " count is: " ++ String.fromInt count
