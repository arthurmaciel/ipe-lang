Add missing State imports for type annotations that reference Model/Msg.

The upstream sky.toml listed crypto/sha256+hex in go.dependencies (Go-backend
internal — not visible in the Ipê source). The Ipê source imports only local
Chess.* / Lib.* / Page.* modules. Three files carry type annotations that
reference Model or Msg from State without importing it; Sky's type-checker
inferred the types, but Ipê requires the import to be in scope. Add the
minimal import to each annotation site.

--- a/src/Chess/Move.ipe
+++ b/src/Chess/Move.ipe
@@ -7,6 +7,7 @@
 import Ipe.String as String
 import Chess.Piece exposing (..)
 import Chess.Board as Board
+import State exposing (Model)


 -- ======================================================
--- a/src/Chess/Ai.ipe
+++ b/src/Chess/Ai.ipe
@@ -9,6 +9,7 @@
 import Chess.Board as Board
 import Chess.Move as Move
 import Chess.Eval as Eval
+import State exposing (Model)


 bestMove : Dict Int Piece -> Model -> ( Int, Int )
--- a/src/Lib/GameLogic.ipe
+++ b/src/Lib/GameLogic.ipe
@@ -11,6 +11,7 @@
 import Chess.Move as Move
 import Chess.Ai as Ai
 import Lib.Games as Games
+import State exposing (Model, Msg)
 import Ipe.Error as Error exposing (Error(..), ErrorKind(..))
