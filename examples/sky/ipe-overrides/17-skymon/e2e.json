{
    "_comment": "17-skymon contract. Verifies the dashboard renders its overview cards. Deeper monitor/metric/alert flows drive HTTP probes and periodic ticks that an extended harness would exercise; for this iteration: catch crashes, ensure the dashboard loads, and alert on any panic in the server log.",
    "kind": "live",
    "port": 8000,
    "startupWaitMs": 3000,
    "steps": [
        {
            "name": "dashboard renders",
            "method": "GET",
            "path": "/",
            "expectStatus": 200,
            "expectBodyContains": ["SkyMon", "Monitors"],
            "expectStderrAbsent": [
                "panic:",
                "FATAL"
            ]
        }
    ]
}
