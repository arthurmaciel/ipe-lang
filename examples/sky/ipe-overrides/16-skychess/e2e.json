{
    "_comment": "16-skychess contract. Verifies the home page renders with the name-entry form. Deeper game-state testing (drive through the Web event dispatch and capture move quality) would need an extended e2e harness. For this iteration: catch crashes, ensure the home page loads, and alert on any panic in the server log.",
    "kind": "live",
    "port": 8000,
    "startupWaitMs": 3000,
    "steps": [
        {
            "name": "home renders",
            "method": "GET",
            "path": "/",
            "expectStatus": 200,
            "expectBodyContains": ["home-title", "Enter your name"],
            "expectStderrAbsent": [
                "panic:",
                "FATAL"
            ]
        }
    ]
}
