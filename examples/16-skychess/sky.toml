name = "ipehess"
version = "0.1.0"
entry = "src/Main.ipe"

[source]
root = "src"

[live]
port = 8000
store = "sqlite"
storePath = "ipehess.db"

[database]
driver = "sqlite"
path = "ipehess.db"

["go.dependencies"]
"crypto/sha256" = "latest"
"encoding/hex" = "latest"
