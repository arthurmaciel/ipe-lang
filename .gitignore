/target
sky-out/
.skycache/
.skydeps/
/tmp-parity/

# Pinned Go-reference oracle binaries (v0.17.3) — large, fetched not committed
tools/oracle/bin/

# agent worktrees — never commit
.claude/worktrees/
