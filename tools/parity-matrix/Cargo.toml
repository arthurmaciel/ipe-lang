[package]
name = "parity-matrix"
edition.workspace = true
version.workspace = true
license.workspace = true
description = "Symbol × layer parity matrix: Sky stdlib vs Rust backend coverage"

[[bin]]
name = "parity-matrix"
path = "src/main.rs"

[dependencies]
# No deps beyond std — all parsing is line-level string ops on known source shapes.

[lints.rust]
unsafe_code = "forbid"
