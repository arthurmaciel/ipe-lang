module Main exposing (main)

import Lib
import Ipe.Io as Io

main =
    let a = if Lib.evenLen [90, 35, 32, 77] then "E" else "O" in
    let b = if Lib.evenLen [ "ors" ] then "E" else "O" in
    Io.println (a ++ b)
