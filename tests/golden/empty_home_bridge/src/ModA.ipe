module ModA exposing (Token(..), makeA, useA)

import Ipe.String as String

type Token = TokA String

makeA : Token
makeA = TokA "a"

useA : Token -> String
useA t =
    case t of
        TokA s -> s
