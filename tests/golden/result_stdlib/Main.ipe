module Main exposing (main)
import Ipe.Io as Io

describe : Result String Int -> Int
describe r =
    case r of
        Ok n -> n
        Err _ -> 0

main =
    Io.println (String.fromInt (describe (Ok 7)))
