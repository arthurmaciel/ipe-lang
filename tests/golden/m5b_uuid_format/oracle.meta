# Cached Go-oracle metadata. Regenerate with the refresh-oracle tool.
main_sky_sha256 = a9981be6d2643f922b5e4fd8d588075ebd91ee45d87c78013a3285d6a79beb15
go_sky_version = sky dev
exit_code = 0
oracle_divergence = true
divergence_reason = sanctioned: Sky-Rust types Uuid.v4 / Uuid.v7 on the EFFECT tier (() -> Task Error String) because entropy is not a memoizable pure value (task #54) — this makes "entropy typed as a pure String" unrepresentable. The Go reference still types them as bare `Uuid.v4 : String` (Limitation #7), so this Task-sequenced program is not co-typable with the Go backend and cannot be Go-oracled; Sky-Rust records its own (semantically correct) output "ok". Uuid.parse remains the pure String -> Maybe String parser on both backends.
