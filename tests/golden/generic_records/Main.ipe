module Main exposing (main)
import Ipe.Prelude exposing (..)
import Ipe.Io as Io

wrap : a -> { value : a }
wrap x = { value = x }

unwrap : { value : a } -> a
unwrap r = r.value

main =
    Io.println (String.fromInt (unwrap (wrap 42)))
