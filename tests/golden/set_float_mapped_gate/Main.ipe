module Main exposing (main)

import Ipe.List as List
import Ipe.Set as Set
import Ipe.Io as Io


main =
    let
        floats =
            List.map (\n -> n + 0.5) [ 1.0, 2.0, 3.0 ]

        s =
            Set.fromList floats
    in
    Io.println (String.fromInt (Set.size s))
