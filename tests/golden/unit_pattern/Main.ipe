module Main exposing (main)
import Ipe.Prelude exposing (..)

-- The unit value `()` as a function argument; `useUnit` ignores it.
useUnit : () -> Int
useUnit u = 7

main =
    println (String.fromInt (useUnit ()))
