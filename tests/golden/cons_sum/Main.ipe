module Main exposing (main)
import Ipe.Io as Io

sum : List Int -> Int
sum xs =
    case xs of
        [] -> 0
        x :: rest -> x + sum rest

main =
    Io.println (String.fromInt (sum [1, 2, 3]))
