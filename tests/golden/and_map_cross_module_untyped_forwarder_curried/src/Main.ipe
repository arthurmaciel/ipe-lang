module Main exposing (main)

import Ipe.Prelude exposing (..)
import Lib exposing (am2)
import Ipe.Io as Io

add3 : Int -> Int -> Int -> Int
add3 a b c =
    a + b + c

main =
    case am2 (Ok 1) (Ok add3) of
        Ok _ -> Io.println "ok"
        Err _ -> Io.println "err"
