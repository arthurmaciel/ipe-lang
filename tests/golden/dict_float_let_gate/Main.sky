module Main exposing (main)

import Sky.Core.Dict as Dict


main =
    let
        prices =
            Dict.fromList [ (1.5, "cheap"), (9.99, "mid") ]
    in
    println (String.fromInt (Dict.size prices))
