module Main exposing (main)
import Ipe.Io as Io

-- Regression gate: `path` is a contextual keyword, not a hard reserved word.
-- When NOT followed immediately by a string literal, `path` is a plain
-- identifier that can be bound and used normally.

path : String
path =
    "not a path literal"

main =
    Io.println path
