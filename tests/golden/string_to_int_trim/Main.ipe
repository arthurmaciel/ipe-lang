module Main exposing (main)
import Ipe.Io as Io
import Ipe.String

main =
    case String.toInt " 42 " of
        Just n ->
            Io.println (String.fromInt n)

        Nothing ->
            Io.println "Nothing"
