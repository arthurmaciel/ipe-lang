module Main exposing (main)

import Ipe.Prelude exposing (..)
import Ipe.String as String
import Ipe.Io as Io

type Color = Red | Green | Blue

label : Color -> String
label c =
    case c of
        Red -> "red"
        _ as w -> "other"
        Blue -> "blue"

main =
    Io.println (label Green)
