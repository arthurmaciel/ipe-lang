module Main exposing (main)
import Ipe.Io as Io
import Ipe.String

firstOr0 : List Int -> Int
firstOr0 xs =
    case xs of
        x :: rest -> x

main =
    Io.println (String.fromInt (firstOr0 [1, 2, 3]))
