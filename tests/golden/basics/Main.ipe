module Main exposing (main)
import Ipe.System as System

type Msg = Increment | Decrement

update : Msg -> Int -> Int
update msg count =
    case msg of
        Increment -> count + 1
        Decrement -> count - 1

main =
    System.setenv "HOME" "x"
