module Main exposing (main)
import Ipe.Io as Io
import Ipe.String

unwrap : Maybe Int -> Int
unwrap m =
    case m of
        Just n -> n
        Nothing -> 0

main =
    Io.println (String.fromInt (unwrap (Just 42)))
