module Main exposing (main)
import Ipe.Prelude exposing (..)
import Ipe.Io as Io

-- A literal arm AFTER a wildcard catch-all can never match — it is redundant
-- (IPE-T0011).
f : Int -> Int
f n =
    case n of
        _ -> 0
        1 -> 1

main =
    Io.println (String.fromInt (f 7))
