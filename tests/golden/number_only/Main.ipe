module Main exposing (main)
import Ipe.Io as Io
import Ipe.String

-- POSITIVE CONTROL (AUD-12): x has only a Number obligation (no Append).
-- Must still default to Int after the fix.
f x = x + 1

main =
    Io.println (String.fromInt (f 5))
