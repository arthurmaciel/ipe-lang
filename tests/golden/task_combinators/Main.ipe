module Main exposing (main)
import Ipe.Io as Io
import Ipe.String
import Ipe.Task

-- Task.succeed wraps a pure value into a Task; Task.andThen chains effects.
main =
    Task.andThen
        (\n -> Io.println (String.fromInt n))
        (Task.succeed 42)
