module Main exposing (main)
import Ipe.Prelude exposing (..)
import Ipe.Io as Io

-- Precedence: `+` (prec 6) binds tighter than `|>` (prec 0).
-- `2 + 3 |> String.fromInt` groups as `(2+3) |> String.fromInt` = "5".
main =
    2 + 3 |> String.fromInt |> Io.println
