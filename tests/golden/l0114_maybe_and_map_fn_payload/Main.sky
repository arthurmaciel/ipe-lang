module Main exposing (main)
import Sky.Core.Prelude exposing (..)

-- #90 Stage 1: `Just f` holding a function used to trip SKY-L0114
-- unconditionally, making `Maybe.andMap` unusable.
main =
    println (String.fromInt (Maybe.withDefault 0 (Just (\x -> x * 2) |> Maybe.andMap (Just 21))))
