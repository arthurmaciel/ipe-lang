module Main exposing (main)
import Ipe.Prelude exposing (..)
import Ipe.Io as Io

-- Character literal pattern with a wildcard catch-all.
grade : Char -> Int
grade c =
    case c of
        'a' -> 1
        'b' -> 2
        _ -> 0

main =
    Io.println (String.fromInt (grade 'b'))
