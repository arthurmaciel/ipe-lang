module Main exposing (main)

import Ipe.Io as Io

-- #32 review follow-up: a mis-arity `Sub` in a CTOR PAYLOAD (`Sub` takes
-- exactly one message type) used to ICE via lower_enum → ir_type_from_canon.
-- lower_enum's Gate 0a now fails closed with IPE-T0016 at the ctor span.
type J
    = J (Sub Int Bool)


main =
    Io.println "x"
