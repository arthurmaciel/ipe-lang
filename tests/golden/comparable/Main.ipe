module Main exposing (main)
import Ipe.Io as Io
import Ipe.String

maxOf : a -> a -> a
maxOf p q = if p > q then p else q

main =
    let bigInt = maxOf 3 7
        bigChar = maxOf 'a' 'z'
        bonus = if bigChar > 'y' then bigInt else 0
    in
    Io.println (String.fromInt bonus)
