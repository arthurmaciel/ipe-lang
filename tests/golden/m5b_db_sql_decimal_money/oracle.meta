# Cached Go-oracle metadata. Regenerate with the refresh-oracle tool.
main_sky_sha256 = 3f4a584e9f68491fa43a489eabafe4ac6e58afb697eabc5a98c17253e0bc3025
go_sky_version = sky dev
exit_code = 0
oracle_divergence = true
divergence_reason = sanctioned: this program uses SqlDecimal + SqlMoney params in Db.exec + Db.query — the Go backend and ipê share the Sky stdlib surface but the Go reference emits Go+SQLite binaries while ipê emits Rust+sqlx; the SQLite in-memory behaviour and row-type representation differ enough that a shared Main.sky cannot run on both backends. ipê's output is recorded as the correct reference.
