module Main exposing (main)
import Ipe.Io as Io
import Ipe.String

type alias Box = { value : Int }

wrap : Int -> Box
wrap n =
    { value = n }

main =
    let
        r = { value = 1 }
    in
    Io.println (String.fromInt ((wrap 41).value + (r).value))
