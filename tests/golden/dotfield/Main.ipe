module Main exposing (main)
import Ipe.Prelude exposing (..)

type alias Box = { value : Int }

wrap : Int -> Box
wrap n =
    { value = n }

main =
    let
        r = { value = 1 }
    in
    println (String.fromInt ((wrap 41).value + (r).value))
