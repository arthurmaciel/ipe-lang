module Main exposing (main)
import Ipe.Io as Io
import Ipe.String

firstOf : (Int, Int) -> Int
firstOf t = if t == (41, 7) then 41 else 0

secondOf : (Int, Int) -> Int
secondOf t = if t == (41, 7) then 7 else 0

main =
    Io.println (String.fromInt (firstOf (41, 7) + secondOf (41, 7)))
