# Cached Go-oracle metadata. Regenerate with the refresh-oracle tool.
main_sky_sha256 = e23e4e9d8406b3639ec0041dc613df885571caede1db087e09b97b95ea3318ab
go_sky_version = sky dev
exit_code = 0
oracle_divergence = true
divergence_reason = divergence: Sky.Core.Jwt flat kernels (encodeRs256/decodeRs256) are the Rust-backend M5b interim surface; the Go backend exposes only the builder API (Jwt.encode / rs256 / decode), so this exact program does not compile on the Go reference. Token bytes remain byte-identical to Go (see tests/golden/m5b_jwt_rs256_bytes). Sky-Rust records its own output. Divergence from Sky, rationale: JWT API-surface interim (docs/architecture/divergence-policy.md).
