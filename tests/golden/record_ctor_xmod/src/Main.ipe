module Main exposing (main)

import Ipe.String as String
import Ipe.Io as Io
import State exposing (Account)


acct : Account
acct =
    Account 42 "root"


main =
    Io.println (String.fromInt acct.id ++ ":" ++ acct.name)
