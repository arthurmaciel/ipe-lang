module Main exposing (main)

import Ipe.Set as Set
import Ipe.Io as Io


addTo : a -> Set a -> Set a
addTo x s =
    Set.insert x s


main =
    Io.println (String.fromInt (Set.size (addTo 4 (Set.fromList [ 1, 2, 3 ]))))
