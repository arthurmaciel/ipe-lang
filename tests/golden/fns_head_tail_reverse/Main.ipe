module Main exposing (main)
import Ipe.Io as Io
import Ipe.List
import Ipe.Maybe
import Ipe.String

main =
    Io.println (String.fromInt (Maybe.withDefault 0 (List.head (List.reverse (Maybe.withDefault [] (List.tail [1, 2, 3]))))))
