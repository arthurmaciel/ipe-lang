# Cached Go-oracle metadata. Regenerate with the refresh-oracle tool.
main_ipe_sha256 = c7ffaab721fb7ec6ee23aa1dc0a7fb2581f5239d9958ffd45bc865471288a3e9
go_sky_version = sky dev
exit_code = 0
oracle_divergence = true
divergence_reason = Go oracle failed: Go `sky build` exited Some(1): -- Discovering modules -- PARSE ERROR ─────────────────────────────────────── Main.ipe:8:41 [E0001] 6 | -- through a tuple rather than a bare self-field. The backend boxes the cyclic 7 | -- enum-payload edge (the whole tuple) so the Rust enum stays finite-sized. 8 | type Chain = ChainEnd | ChainNode (Chain, Int) | ^ 9 | Top-level declaration expected here. A declaration is `name : Type` (annotation) or `name args = body` (value) or `type alias Foo = …` / `type Foo = A | B`.; using skyc output as the reference
