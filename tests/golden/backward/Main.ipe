module Main exposing (main)
import Ipe.Io as Io

-- Backward pipe `<|`: right-associative at precedence 0.
-- `<|` is looser than `+`, so `1 + 2` groups first.
main =
    Io.println <| String.fromInt <| 1 + 2
