module Main exposing (main)

-- Effect sequencing: `let _ = task in rest` fires the F1 auto-force rule,
-- turning the discarded Task into a `task_and_then` chain so both effects run.
main =
    let
        _ = println "step 1"
    in
    println "step 2"
