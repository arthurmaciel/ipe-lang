module Main exposing (main)
import Ipe.Io as Io
import Ipe.Task as Task

-- Effect sequencing: two effects threaded with `Task.andThen`, ignoring the
-- first task's result, so both effects run in order.
main =
    Io.println "step 1"
        |> Task.andThen (\_ -> Io.println "step 2")
