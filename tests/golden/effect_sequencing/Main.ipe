module Main exposing (main)
import Ipe.Io as Io
import Ipe.Task as Task

-- Effect sequencing: sequencing two effects via `do` notation desugars to the
-- same `task_and_then` chain — both effects run in order.
main =
    do
        Io.println "step 1"
        Io.println "step 2"
