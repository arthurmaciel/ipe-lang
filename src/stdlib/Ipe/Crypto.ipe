-- | Ipe.Crypto — cryptographic primitives.
--
-- All functions are aliased to runtime kernels via `Ffi.kernel`.
-- Security notes:
--   * `constantTimeEqual` stays constant-time (backed by `subtle`).
--   * AEAD functions (`aesGcmEncrypt`, `chacha20Encrypt`) use a fresh
--     random nonce on every call; the nonce is prepended to the
--     ciphertext so `decrypt` can recover it.
--   * Key/secret material is never logged.
--   * `Key` and `Mac` enforce role at compile time: `hmacSha256` and
--     `hmacSha512` require a `Key`; passing a bare `String` (e.g. a
--     plaintext message) where a key is expected is a compile error.
--     Construct a `Key` via `keyFromString` or `keyFromBytes`.
module Ipe.Crypto exposing
    ( sha256, sha512, sha1, md5
    , rsaSha256Sign, rsaSha256Verify
    , constantTimeEqual
    , aesGcmEncrypt, aesGcmDecrypt
    , chacha20Encrypt, chacha20Decrypt
    , aesKeyFromPassword, chachaKeyFromPassword
    , randomBytes, randomToken
    , Key
    , Mac
    , keyFromString
    , keyFromBytes
    , macToHex
    , hmacSha256
    , hmacSha512
    , aesGcmEncryptKey
    , aesGcmDecryptKey
    , chacha20EncryptKey
    , chacha20DecryptKey
    , aesKeyFromPasswordKey
    , chachaKeyFromPasswordKey
    )


import Ipe.Ffi as Ffi


-- ── Hashes ───────────────────────────────────────────────────────────────────

sha256 : String -> String
sha256 = Ffi.kernel "Crypto_sha256"

sha512 : String -> String
sha512 = Ffi.kernel "Crypto_sha512"

sha1 : String -> String
sha1 = Ffi.kernel "Crypto_sha1"

md5 : String -> String
md5 = Ffi.kernel "Crypto_md5"


-- ── HMAC ─────────────────────────────────────────────────────────────────────
--
-- Both functions require a `Key`.  Construct one via `keyFromString` or
-- `keyFromBytes` at your parse boundary.  Passing a bare `String` where a
-- `Key` is expected is a compile error, making message-as-key impossible.

-- | `hmacSha256 key message` — HMAC-SHA-256.  Returns a typed `Mac`.
hmacSha256 : Key -> String -> Mac
hmacSha256 = Ffi.kernel "Crypto_hmacSha256WithKey"

-- | `hmacSha512 key message` — HMAC-SHA-512.  Returns a typed `Mac`.
hmacSha512 : Key -> String -> Mac
hmacSha512 = Ffi.kernel "Crypto_hmacSha512WithKey"


-- ── RSA / asymmetric ─────────────────────────────────────────────────────────

rsaSha256Sign : String -> String -> Result Error String
rsaSha256Sign = Ffi.kernel "Crypto_rsaSha256Sign"

rsaSha256Verify : String -> String -> String -> Bool
rsaSha256Verify = Ffi.kernel "Crypto_rsaSha256Verify"


-- ── Constant-time comparison ──────────────────────────────────────────────────

constantTimeEqual : String -> String -> Bool
constantTimeEqual = Ffi.kernel "Crypto_constantTimeEqual"


-- ── AEAD symmetric encryption ─────────────────────────────────────────────────

aesGcmEncrypt : String -> String -> Result Error String
aesGcmEncrypt = Ffi.kernel "Crypto_aesGcmEncrypt"

aesGcmDecrypt : String -> String -> Result Error String
aesGcmDecrypt = Ffi.kernel "Crypto_aesGcmDecrypt"

chacha20Encrypt : String -> String -> Result Error String
chacha20Encrypt = Ffi.kernel "Crypto_chacha20Encrypt"

chacha20Decrypt : String -> String -> Result Error String
chacha20Decrypt = Ffi.kernel "Crypto_chacha20Decrypt"


-- ── Key derivation ────────────────────────────────────────────────────────────

aesKeyFromPassword : String -> String -> String
aesKeyFromPassword = Ffi.kernel "Crypto_aesKeyFromPassword"

chachaKeyFromPassword : String -> String -> String
chachaKeyFromPassword = Ffi.kernel "Crypto_chachaKeyFromPassword"


-- ── Random ───────────────────────────────────────────────────────────────────

randomBytes : Int -> Task Error String
randomBytes = Ffi.kernel "Crypto_randomBytes"

randomToken : Int -> Task Error String
randomToken = Ffi.kernel "Crypto_randomToken"


-- ── Typed newtypes ────────────────────────────────────────────────────────────
--
-- `Key` and `Mac` are opaque types backed by runtime newtypes.
-- They enforce role at the type level: passing a `Mac` where a `Key`
-- is expected is a compile error.  Key material never appears in logs.

-- | An opaque cryptographic key.  Construct only via `keyFromString`
-- or `keyFromBytes`; never forge from a plain string.
type Key = Key

-- | An opaque message authentication code (HMAC output).  Inspect via
-- `macToHex`; never compare with `==` (use `constantTimeEqual` on the
-- hex representation instead, or use a typed constant-time check).
type Mac = Mac

-- | Parse a hex-encoded or raw string into a `Key`.  Returns
-- `Nothing` if the byte length is wrong for any supported algorithm.
keyFromString : String -> Maybe Key
keyFromString = Ffi.kernel "Key_fromString"

-- | Parse raw bytes (as Ipe `String`/`Bytes`) into a `Key`.
keyFromBytes : String -> Maybe Key
keyFromBytes = Ffi.kernel "Key_fromBytes"

-- | Render a `Mac` as a lowercase hex string for storage or display.
macToHex : Mac -> String
macToHex = Ffi.kernel "Mac_toHex"


-- ── Typed AEAD ───────────────────────────────────────────────────────────────

-- | `aesGcmEncryptKey key plaintext` — AES-256-GCM encrypt with a typed `Key`.
aesGcmEncryptKey : Key -> String -> Result Error String
aesGcmEncryptKey = Ffi.kernel "Crypto_aesGcmEncryptKey"

-- | `aesGcmDecryptKey key ciphertext` — AES-256-GCM decrypt with a typed `Key`.
aesGcmDecryptKey : Key -> String -> Result Error String
aesGcmDecryptKey = Ffi.kernel "Crypto_aesGcmDecryptKey"

-- | `chacha20EncryptKey key plaintext` — ChaCha20-Poly1305 encrypt with a typed `Key`.
chacha20EncryptKey : Key -> String -> Result Error String
chacha20EncryptKey = Ffi.kernel "Crypto_chacha20EncryptKey"

-- | `chacha20DecryptKey key ciphertext` — ChaCha20-Poly1305 decrypt with a typed `Key`.
chacha20DecryptKey : Key -> String -> Result Error String
chacha20DecryptKey = Ffi.kernel "Crypto_chacha20DecryptKey"


-- ── Typed key derivation ──────────────────────────────────────────────────────

-- | Derive an AES-256 `Key` from a password + salt string.
aesKeyFromPasswordKey : String -> String -> Key
aesKeyFromPasswordKey = Ffi.kernel "Crypto_aesKeyFromPasswordKey"

-- | Derive a ChaCha20 `Key` from a password + salt string.
chachaKeyFromPasswordKey : String -> String -> Key
chachaKeyFromPasswordKey = Ffi.kernel "Crypto_chachaKeyFromPasswordKey"
