-- | Ipe.Bitwise — bitwise operations on `Int`.
--
-- Mirrors elm/core `Bitwise`.  Each function is aliased to its runtime
-- kernel via the `Ffi.kernel` mechanism.
--
-- `Int` is 64-bit two's-complement here, so `complement` and
-- `shiftRightZfBy` cover the full 64-bit width rather than wrapping at
-- Elm's 32 bits.  Shift amounts are taken modulo the word width.
module Ipe.Bitwise exposing
    ( and, or, xor, complement
    , shiftLeftBy, shiftRightBy, shiftRightZfBy
    )


import Ipe.Ffi as Ffi


-- | Bitwise AND.
and : Int -> Int -> Int
and = Ffi.kernel "Bitwise_and"


-- | Bitwise OR.
or : Int -> Int -> Int
or = Ffi.kernel "Bitwise_or"


-- | Bitwise XOR.
xor : Int -> Int -> Int
xor = Ffi.kernel "Bitwise_xor"


-- | Bitwise complement (flip every bit).
complement : Int -> Int
complement = Ffi.kernel "Bitwise_complement"


-- | Arithmetic left shift.  `shiftLeftBy offset n` shifts `n` left by
-- `offset` bits.
--
--     Bitwise.shiftLeftBy 1 5 == 10
shiftLeftBy : Int -> Int -> Int
shiftLeftBy = Ffi.kernel "Bitwise_shiftLeftBy"


-- | Arithmetic (sign-preserving) right shift.
--
--     Bitwise.shiftRightBy 1 32 == 16
shiftRightBy : Int -> Int -> Int
shiftRightBy = Ffi.kernel "Bitwise_shiftRightBy"


-- | Zero-fill (logical) right shift: the sign bit is not replicated.
shiftRightZfBy : Int -> Int -> Int
shiftRightZfBy = Ffi.kernel "Bitwise_shiftRightZfBy"
