[project]
name = "sp2-manifest-fixture"

[dependencies]
http  = "^1.2"
mylib = { git = "https://example.com/mylib.git", rev = "abc123" }
local = { path = "../local" }

[rust.dependencies]
uuid   = "1.10"
stripe = { version = "=1.0.0", features = ["blocking", "webhooks"] }

[capabilities]
declared = ["network", "clock"]
