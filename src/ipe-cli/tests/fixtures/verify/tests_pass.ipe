module Main exposing (main)

import Ipe.Test as Test exposing (Test)


tests : List Test
tests =
    [ Test.suite "arithmetic"
        [ Test.test "addition" (\_ -> Test.equal 4 (2 + 2))
        , Test.test "subtraction" (\_ -> Test.equal 0 (1 - 1))
        ]
    ]


main =
    Test.runMain tests
