module Package exposing (package)

import Ipe.Package exposing (..)


package : Package
package =
    { name = "{name}"
    , version = "0.1.0"
    , delivery =
        { {ships}desktop = { title = "{name}", width = 1024, height = 768 }
        , mobile = { bundleId = "com.example.{name}", orientation = Portrait }
        , browser = { basePath = "/" }
        }
    }
