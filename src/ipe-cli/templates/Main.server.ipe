module Main exposing (main)

import Ipe.Server.Http as Server
import Ipe.Task


handle : Server.Request -> Task Error Server.Response
handle _req =
    Task.succeed (Server.text "Hello, world!")


main =
    Server.listen 8000
        [ Server.get "/" handle
        ]
