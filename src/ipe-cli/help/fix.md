Apply the compiler's machine-applicable fixes to a source file.

```
ipe fix <path>
```

## Arguments

The source file to fix.

## Options

- `[--yes]` — apply every fix without per-edit confirmation
