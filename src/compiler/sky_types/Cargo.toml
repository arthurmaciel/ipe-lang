[package]
name = "sky_types"
edition.workspace = true
version.workspace = true
license.workspace = true

[dependencies]
sky_intern = { path = "../sky_intern" }
sky_diagnostics = { path = "../sky_diagnostics" }
sky_canon = { path = "../sky_canon" }
# Leaf crate (deps: sky_intern + sky_diagnostics only) → clean down-edge, no
# cycle. Needed to name `StdlibKernel` in the parse-once kernel-scheme registry
# (`stdlib_scheme`) that the `VarKernel { id }` constrain arm delegates to.
sky_kernels = { path = "../sky_kernels" }

[dev-dependencies]
sky_parse = { path = "../sky_parse" }

[lints]
workspace = true
