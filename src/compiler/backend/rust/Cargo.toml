[package]
name = "ipe_backend_rust"
edition.workspace = true
version.workspace = true
license.workspace = true

[dependencies]
ipe_ir = { path = "../../ir" }
ipe_intern = { path = "../../intern" }
ipe_backend = { path = ".." }
ipe_diagnostics = { path = "../../diagnostics" }
# Model schema tag (H24) — same pin as ipe_watch/skyc/runtime.
sha2 = "0.10"

[lints]
workspace = true
