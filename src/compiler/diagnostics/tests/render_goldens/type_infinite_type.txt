-- INFINITE TYPE ------------------------------------------- test.ipe

This value's type would have to contain itself forever, so I can't pin it down.

 --> test.ipe:4:8
  |
4 |     let f x = f
  |        ^ `a` would have to equal List a
  |

IPE-T0002 · run `ipe doc IPE-T0002`
