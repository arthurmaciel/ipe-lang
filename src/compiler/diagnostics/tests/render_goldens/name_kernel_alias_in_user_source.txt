-- A KERNEL ALIAS (KERNEL.KERNEL) MAY NOT BE MINTED IN USER SOURCE - test.ipe

This code tries to mint a built-in effect directly, which only the standard library may do.

 --> test.ipe:4:5
  |
4 |     Kernel.kernel "Http_get"
  |     ^^^^^^^^^^^^^^^^^^^^^^^^ `Kernel.kernel "Http_get"` mints a kernel — only the standard library and the generated FFI interface may do this; reach the effect through its published module (an unsafe kernel via its `Ipe.<M>.Unsafe` module, which discloses the `unsafe` capability)
  |

IPE-N0042 · run `ipe doc IPE-N0042`
