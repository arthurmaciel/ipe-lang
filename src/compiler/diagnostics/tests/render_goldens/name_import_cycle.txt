-- NAMING ERROR -------------------------------------------- <no file>

These modules import each other in a circle, so I can't decide which to compile first.

IPE-N0021 · run `ipe explain IPE-N0021`
