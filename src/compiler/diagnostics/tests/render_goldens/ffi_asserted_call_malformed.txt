-- NAMING ERROR -------------------------------------------- test.ipe

I can't read this `Rust.Ffi.call` — it isn't in the one shape I accept.

 --> test.ipe:2:1
  |
2 | 
  | ^ this `Rust.Ffi.call` is malformed: the body must be exactly `Rust.Ffi.call "<crate>::<fn>"`. The one accepted shape is a top-level annotated definition whose whole body is `Rust.Ffi.call "<crate>::<function>"`
  |

IPE-N0038 · run `ipe explain IPE-N0038`
