# IPE-I0200: ICE: no Rust name for symbol

Sorry — the compiler couldn't finish generating code for your program. When
Ipe emits Rust, every type and function it kept needs a corresponding Rust
name. The backend asked for the Rust name of a symbol and got nothing back,
so it couldn't write out the code.

This is a bug in the Ipe Rust compiler, not a mistake in your code. Every
symbol that survives to code generation should have a name reserved for it;
finding one without a name means the backend's bookkeeping got out of step.

A small program that the backend turns into Rust:

```ipe
module Main exposing (main)

main : Task ()
main =
    Io.println "hello"
```

A user-defined type and function, both of which need Rust names:

```ipe
module Main exposing (main)

type Light
    = Red
    | Green

describe : Light -> String
describe light =
    case light of
        Red -> "stop"
        Green -> "go"

main : Task ()
main =
    Io.println (describe Red)
```

This is ordinary Ipe and should generate code without trouble:

```ipe
module Main exposing (main)

double : Int -> Int
double n = n + n

main : Task ()
main =
    Io.println (String.fromInt (double 21))
```

This is a bug in the Ipe Rust compiler — not your fault. Please report it
(with this source + `ipec --version`) at

https://github.com/arthurmaciel/ipe-lang/issues

so we can fix the backend's name table.
